apiVersion: v1
kind: Service
metadata:
    name: web
spec:
  ports:
  - port: 80
    name: web
    protocol: TCP
    targetPort: 80
  type: LoadBalancer
  selector:
    app: web
---
apiVersion: apps/v1
metadata:
  name: web
  labels:
      app: web
kind: StatefulSet
spec:
  serviceName: web-service
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: nginx
        image: nginx:1.8
        volumeMounts:
          - name: my-pvc-nfs
            mountPath: /usr/share/nginx/html
        ports:
        - containerPort: 80
          name: web
      volumes:
      - name: my-pvc-nfs
        persistentVolumeClaim:
          claimName: web-content
